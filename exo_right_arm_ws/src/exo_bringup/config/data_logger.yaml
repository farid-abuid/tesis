# Per-arm topics (arms, telemetry_topics, session_topics, etc.) are set by the launch files.
exo_data_logger:
  ros__parameters:
    log_root: "~/exo_logs"
    use_controller_session: true
    use_lifecycle_events: true
    auto_plot: false
    sample_rate_hz: 200.0
